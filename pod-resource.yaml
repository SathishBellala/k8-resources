apiVersion: v1
kind: Pod
metadata:
  name: pod-nginx
spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        requests:
          cpu: "205m"
          memory: "120Mi"
        limits:
          cpu: "200m"
          memory: "250Mi"
