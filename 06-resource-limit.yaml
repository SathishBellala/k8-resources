apiVersion: v1
kimd: Pod
metadata:
  name: limit
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "70Mi"
        cpu: "100m"
      limits:
        memory: "100Mi"
        cpu: "120m"